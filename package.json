{
  "name": "matrix-platform-ci-support",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "jest": "^29.6.0"
  }
}
{
  "name": "matrix-platform-root-smoke",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "node ./scripts/smoke-root.js"
  }
}

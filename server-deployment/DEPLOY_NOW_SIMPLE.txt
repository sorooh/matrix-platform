🚀 نشر سريع - Matrix Platform v11.0.0
=====================================

📋 الأمر الكامل (انسخ والصق):

ssh root@46.224.42.221 'cd /opt/matrix-platform/matrix-scaffold/backend && git pull origin master && npm install --legacy-peer-deps && npx prisma generate && npm run build && pm2 restart matrix-platform && sleep 5 && pm2 list && curl http://localhost:3000/health'

🔐 كلمة المرور: aiadsham

---

📋 خطوات يدوية (إذا فشل الأمر السابق):

1. الاتصال بالسيرفر:
   ssh root@46.224.42.221

2. الانتقال للمجلد:
   cd /opt/matrix-platform/matrix-scaffold/backend

3. تحديث الكود:
   git pull origin master

4. تثبيت dependencies:
   npm install --legacy-peer-deps

5. توليد Prisma Client:
   npx prisma generate

6. بناء التطبيق:
   npm run build

7. إعادة تشغيل PM2:
   pm2 restart matrix-platform

8. التحقق من الحالة:
   pm2 list
   curl http://localhost:3000/health

---

🔗 الروابط:
- Health: http://46.224.42.221:3000/health
- Domain: https://senorbit.ai/health
